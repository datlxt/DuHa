# Server/backend only.
# Copy this to the backend runtime environment when AI render is implemented.

OPENAI_API_KEY=your_openai_api_key

# Optional future backend settings
OPENAI_IMAGE_MODEL=gpt-image-1
