# Root env used by the Docker dev stack (compose.yaml). Copy to `.env`.
# For host-based dev (npm run dev), use frontend/.env instead.
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Port the dev server is published on (optional)
HTTP_PORT=5173
